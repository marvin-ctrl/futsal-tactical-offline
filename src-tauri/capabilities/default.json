{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for local desktop usage",
  "windows": ["main"],
  "permissions": ["core:default"]
}
